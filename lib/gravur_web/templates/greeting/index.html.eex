<h1>Greetings</h1>
<%= for greeting <- @book.greetings do %>
  <%= greeting.text %>
  <img src="<%= Gravur.Utils.Image.url({ greeting.image, greeting }) %>" width="300" />
  </br>
<% end %>
</br>
<%= link "New greeting", to: Routes.book_greeting_path(@conn, :new, @book), class: "btn btn-outline-primary ml-md-3" %>