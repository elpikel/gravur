<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Gravur</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto&display=swap" rel="stylesheet">
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light">
        <a class="navbar-brand app-logo" href="/">Gravur</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
          <ul class="navbar-nav">
            <li class="nav-item pl-2 mt-2">
              <span class="nav-link disabled">O NAS</span>
            </li>
            <li class="nav-item pl-2 mt-2">
              <span class="nav-link disabled">JAK TO DZIAŁA?</span>
            </li>
            <li class="nav-item pl-2 mt-2">
              <span class="nav-link disabled">WYBÓR PAKIETU</span>
            </li>
            <li class="nav-item pl-2 mt-2">
              <%= link "ZALOGUJ", to: Routes.session_path(@conn, :new), class: "btn px-4 py-2 app-button" %>
            </li>
            <li class="nav-item pl-2 mt-2">
              <%= link "ZAŁÓŻ KONTO", to: Routes.registration_path(@conn, :new), class: "btn px-4 py-2 app-button" %>
            </li>
          </ul>
        </div>
      </nav>
    </header>
    <main role="main" class="container mt-5">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
  </body>
</html>
